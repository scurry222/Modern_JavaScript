// Add functions as propertied on objects
// Also called methods

// Object to group similar math functions
const math = {
    multiply(x, y){
        return x * y;
    },
    divide(x, y){
        return x / y;
    },
    square(x){
        return x * x;
    }
}
